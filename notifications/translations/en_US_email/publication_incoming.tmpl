<p>New publication available! '{{.title}}'</p>

<table class="publication_incoming">
  <tbody>
    <tr><td><strong>Description</strong></td><td class="textasis">{{ .publication_description }}</td></tr>
    <tr></tr>
    <tr><td><strong>Case file</strong></td><td>{{ .casefile_name }}</td></tr>
    <tr><td><strong>Ref. number</strong></td><td>{{ .ref_number }}</td></tr>
    <tr><td><strong>Observation date</strong></td><td>{{ .casefile_date }}</td></tr>
    <tr><td><strong>Location</strong></td><td>{{ .casefile_location }}</td></tr>
    <tr><td><strong>Severity</strong></td><td><span class="severity {{ .casefile_severity }}">{{ .casefile_severity }}</span></td></tr>

    {{ range $key, $att := .attributes }}
      <tr><td><strong>{{ $att.Title }}</strong></td><td>{{ $att.Value }}</td></tr>
    {{ end }}

    {{ $length := len .observations }} {{ if gt $length 0 }}
    <tr>
        <td><strong>Observers</strong></td>
        <td>
          <ul>
            {{ range $key, $value := .observations }}<li>{{ $value }}</li>{{ end }}
            </ul>
        </td>
    </tr>
    {{ end }}

    {{ $length := len .witnesses }} {{ if gt $length 0 }}
    <tr>
        <td><strong>Witnesses</strong></td>
        <td>
          <ul>
            {{ range $key, $value := .witnesses }}<li>{{ $value.Name }}</li>{{ end }}
            </ul>
        </td>
    </tr>
    {{ end }}

    {{ $length := len .actions }} {{ if gt $length 0 }}
    <tr>
        <td><strong>Actions</strong></td>
        <td>
          <ul>
            {{ range $key, $value := .actions }}<li>{{ $value }}</li>{{ end }}
            </ul>
        </td>
    </tr>
    {{ end }}
  </tbody>
</table>
